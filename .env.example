# URL pública da API .NET (EC2 / produção)
# Na Vercel: Project → Settings → Environment Variables
NEXT_PUBLIC_API_URL=http://54.91.138.246:5080

# Só para `next start`/`next dev` local com proxy /backend (opcional)
# API_INTERNAL_URL=http://127.0.0.1:5080
